apiVersion: v1
kind: Service
metadata:
  name: matter-server
  namespace: matter-server
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: matter-server
  ports:
    - name: websocket
      port: 5580
      targetPort: 5580
      protocol: TCP
