apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: multus-cni
  namespace: kube-system
spec:
  interval: 15m
  chart:
    spec:
      chart: multus-cni
      version: 2.12.0
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
      interval: 15m
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
      remediateLastFailure: true
  values:
    # Keep in step with the node's Talos version; this image is where the
    # reference CNI binaries below are copied from.
    talosCniImage:
      repository: ghcr.io/siderolabs/cni
      tag: v1.14.0@sha256:717328255ca6969bc225d7788ebc05c95ed3d4179dd38a804e438bbd38f14d2e
    multus:
      integrations:
        talos:
          enabled: true
          # Enabling the Talos integration hands the chart ownership of these
          # binaries in /opt/cni/bin - it deletes them on uninstall.
          installCni:
            macvlan: true
            ipvlan: true
            tuning: true
